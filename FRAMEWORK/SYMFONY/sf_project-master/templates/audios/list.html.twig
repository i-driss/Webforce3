{% extends 'base.html.twig' %}

{% block title %}Hello AudiosController!{% endblock %}

{% block body %}
<section class="row justify-content-center">
    <div class="col-10">
        <div class="row">
            {% for audio in audios %}
            <div class="col-5">
                <div class="card">
                    <div class="card-body">
                        <h6 class="card-title">{{ audio.getTitle() }}</h6>
                        
                        <a href="{{ path('audios_view', {id: audio.getId()}) }}" class="btn btn-outline-info">♫ Ecouter</a>
                    </div>
                </div>
            </div>
            {% endfor %}
        </div>
    </div>

</section>
{% endblock %}
